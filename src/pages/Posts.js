import "../styles/postsStyles.scss";

const Posts = () => {
  return {
    /* {posts.map((post) => {
          const { id, title, body, created_at } = post;

          return (
            <div className="post" key={id}>
              <h2>{title}</h2>
              <p>{body}</p>
              <span className="date">{created_at}</span>
            </div>
          );
        })} */
  };
};

export default Posts;
